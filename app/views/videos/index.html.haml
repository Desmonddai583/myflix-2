- @categories.each do |c| 
  %article.video_category  
    %header
      %h3= link_to c.name, c
    .videos.row
      - c.videos.each do |v|
        .video.col-sm-2
          = link_to v, nil do
            %img(src="#{v.small_cover_url}")